import { CountWidget } from "@widgets/count";

function CountPage() {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <CountWidget />
    </div>
  );
};

export default CountPage;